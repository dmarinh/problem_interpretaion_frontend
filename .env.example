# Required — URL of the locally running FastAPI backend
VITE_API_BASE_URL=http://localhost:8000

# Optional — cache mode: "off" (default) or "session"
# off:     every submit re-runs the backend; every URL visit re-fetches
# session: responses cached in-memory for the browser session; URL revisits are instant
VITE_CACHE_MODE=off
